

export interface Iproduct {
    templateId?: string,
    name: string,
    region: string,
    modifidedBy?: Date,
    modifiedOn?: Date,
    formArray ?: Array<any>,
    id ? : string
}

export interface IsignUp{
    userName : string,
    email : string, 
    password : string,
    id ? : string
}

