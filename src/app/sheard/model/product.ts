

export interface Iproduct {
    name: string,
    region: string,
    modifidedBy ?: string,
    modifiedOn?: string,
    formArray?: Array<any>,
    templateId?: string,
    id ? : string
}

export interface IsignUp{
    userName : string,
    email : string, 
    password : string,
    id ? : string
}

